/**
 * Test stub for the `server-only` package.
 *
 * Its production entry point throws when resolved outside a React Server
 * Component graph, which would break unit tests that import server modules
 * directly. Aliased in vitest.config.ts.
 */
export {};
